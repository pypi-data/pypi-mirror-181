[tool.poetry]
name = "python-ssdb"
version = "v1.0.1"
description = "Python implementation of SSDB client"
authors = ["lsh918 <vx0918@gmail.com>"]
readme = "README.md"
repository = "https://github.com/lsh918/python-ssdb"
packages = [{include = "ssdb"}]

[tool.poetry.build]
script = "build_extension.py"

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core", "Cython", "setuptools"]
build-backend = "poetry.core.masonry.api"
