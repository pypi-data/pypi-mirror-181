[tool:pytest]
# addopts = --pdbcls=IPython.terminal.debugger:TerminalPdb --flake8 --cov=./ -m "not performance"

# Disable benchmarks. Benchmarked functions are only ran once and no stats are reported. Use this if you want to run
# the test but don’t do any benchmarking.
#addopts = --benchmark-disable

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    performance: marks tests as performance (deselect with '-m "not performance"')


[flake8]
select = I900
max-line-length = 100
