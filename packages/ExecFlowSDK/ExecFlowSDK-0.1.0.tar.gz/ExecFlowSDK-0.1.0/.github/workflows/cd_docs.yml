### IMPORTANT ###
# GH Pages currently does not work for this repository.
# This means the documentation cannot be deployed, hence the job is commented out.
# Other steps are commented out as well, relating to connecting to GH Pages.

name: CD - Deploy documentation to GH Pages

on:
  push:
    branches: ["master"]
  pull_request:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      # - name: Setup Pages
      #   uses: actions/configure-pages@v2

      - name: Install system dependencies
        run: sudo apt update && sudo apt install -y graphviz

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -U setuptools wheel flit
          pip install -U -e .[docs]

      - name: Build documentation
        run: sphinx-build docs public -W

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: 'public/'

  # deploy:
  #   if: github.event_name != 'pull_request'

  #   # Allow one concurrent deployment
  #   concurrency:
  #     group: "pages"
  #     cancel-in-progress: true

  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}

  #   runs-on: ubuntu-latest
  #   needs: build

  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v1
