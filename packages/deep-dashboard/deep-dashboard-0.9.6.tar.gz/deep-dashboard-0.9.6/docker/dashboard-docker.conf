[DEFAULT]

runtime_dir=/srv/deep-dashboard-runtime

# Log output to standard error. This option is ignored if log_config_append is
# set. (boolean value)
use_stderr = true

[cache]

memcached_ip = memcached
