[tool.poetry]
name = "mbus-gem-decoder"
version = "0.1.0"
description = "MBUS-GEM decoder"
authors = ["René <rene.pihlak@ttu.ee>"]
license = "GPLv3"
readme = "README.md"
packages = [{include = "mbus_gem_decoder"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
