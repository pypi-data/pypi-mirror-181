# conda env create --file environment.yml
# conda env update --file environment.yml --prune
# conda env remove -n mltz-env
name: 'mltz-env'
channels:
  - conda-forge
  - defaults
dependencies:
  - scikit-learn
  - hatch
  - pip
  - pandas
  - pip:
    - twine
    - pytest
    - build
    - hatchling